<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>직원 및 시설 소개</title>
    <link rel="stylesheet" href="/css/admin_facilitiesMain_style.css">
</head>

<body>
    <div id="introduction">
        <h2>직원소개</h2>
        <div class="cover">

            <table class="employee-table">
                <tr>
                    <% staff.forEach((employee, index)=> { %>
                        <td>
                            <div class="employee-card">
                                <img src="<%= employee.photo || 'path/to/default/image.jpg' %>" alt="Profile Image"
                                    class="facility-image">
                                <div class="employee-name">
                                    <%= employee.name %>
                                </div>
                                <ul class="employee-info">
                                    <li>직급: <%= employee.role %>
                                    </li>
                                    <li>연락처: <%= employee.contact_info %>
                                    </li>
                                </ul>
                            </div>
                        </td>
                        <% if ((index + 1) % 3===0) { %>
                </tr>
                <tr>
                    <% } %>
                        <% }); %>
                </tr>
            </table>

        </div>

        <h2>시설소개</h2>
        <div class="cover">
            <div class="facility-table">
                <% facilities.forEach((facility)=> { %>
                    <div class="facility-card">
                        <img src="<%= facility.photo || 'path/to/default/image.jpg' %>" alt="Facility Image"
                            class="facility-image">
                        <div class="facility-name">
                            <%= facility.facility_name %>
                        </div>
                        <ul class="facility-info">
                            <li>주요 시설: <%= facility.main_facilities %>
                            </li>
                        </ul>
                    </div>
                    <% }); %>
            </div>
        </div>

        <div class="btnBox">
        </div>
    </div>
    </div>
</body>

</html>