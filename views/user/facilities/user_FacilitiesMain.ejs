<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>직원 및 시설 소개</title>
    <link rel="stylesheet" href="/css/facilitiesMain_style.css">
</head>

<body>
    <h2>직원소개</h2>
    <div class="container">
        <div class="employee-container">
            <table class="employee-table">
                <tr>
                    <% staff.forEach((employee, index) => { %>
                        <td>
                            <div class="employee-card">
                                <img src="/img/<%= employee.staff_photo %>" alt="Profile Image" class="facility-image">
                                <ul class="employee-info">
                                    <li>이름: <%= employee.name %></li>
                                    <li>직급: <%= employee.role %></li>
                                    <li>경력: <%= employee.contact_info %></li>
                                </ul>
                            </div>
                        </td>
                        <% if ((index + 1) % 3 === 0) { %>
                            </tr><tr>
                        <% } %>
                    <% }); %>
                </tr>
            </table>
        </div>
    </div>

    <h2>시설소개</h2>
    <div class="container facility-container">
        <table class="facility-table">
            <tr>
                <% facilities.forEach((facility, index) => { %>
                    <td>
                        <div class="facility-card">
                            <img src="/img/<%= facility.facility_photo %>" alt="Uploaded Image">       
                            <ul class="facility-info">
                                <li>시설 이름: <%= facility.facility_name %></li>
                                <li>주요 시설: <%= facility.main_facilities %></li>
                            </ul>
                        </div>
                    </td>
                    <% if ((index + 1) % 3 === 0) { %>
                        </tr><tr>
                    <% } %>
                <% }); %>
            </tr>
        </table>
    </div>
</body>
</html>
