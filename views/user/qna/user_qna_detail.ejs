<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= locals.title %></title>
  <link rel="stylesheet" href="/css/qna_styles.css">
</head>
<body>
  <h1>QnA 상세</h1>
  <h2><%= question.title %></h2>
  <p><%= question.question %></p>
  <p>작성일: <%= question.question_date %></p>
  <p>작성자: <%= question.question_by %></p>

  <h3>답변</h3>
  <% answers.forEach(function(answer) { %>
      <div>
          <p><%= answer.answer %></p>
          <p>작성일: <%= answer.answer_date %></p>
          <p>작성자: <%= answer.answered_by %></p>
      </div>
  <% }) %>

  <% if (!answers.length) { %>
      <p>아직 답변이 없습니다.</p>
  <% } %>
    <button onclick="location.href='/qna/edit/<%= question.question_id %>'">수정하기</button>

  <h3>관리</h3>
  <form action="/qna/delete/<%= question.question_id %>" method="POST" onsubmit="return confirm('정말 삭제하시겠습니까?');">
      <button type="submit">질문 삭제</button>
  </form>

  <a href="/qna">목록으로</a>
</body>
</html>