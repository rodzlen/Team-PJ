<!DOCTYPE html>
<html lang="ko">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>
    <%= locals.title %>
  </title>
  <link rel="stylesheet" href="/css/communityDetail_style.css">
</head>

<body>
  <div id="detail">
    <h3>QnA 상세</h3>
    <div id="cover">
      <h2>
        <%= question.title %>
      </h2>
      <p>
        <%= question.question %>
      </p>
      <p>작성일: <%= question.question_date %>
      </p>
      <p>작성자: <%= question.question_by %>
      </p>

      <h4>답변</h4>
      <% answers.forEach(function(answer) { %>
        <div>
          <p>
            <%= answer.answer %>
          </p>
          <p>작성일: <%= answer.answer_date %>
          </p>
          <p>작성자: <%= answer.answered_by %>
          </p>
        </div>
        <% }) %>

          <% if (!answers.length) { %>
            <p>아직 답변이 없습니다.</p>
            <% } %>
              <button id="editBtn" onclick="location.href='/qna/edit/<%= question.question_id %>'">수정하기</button>
    </div>

    <div id="btnBox">
      <button id="backBtn" onclick="location.href='/qna'">목록으로</button>
      <form action="/qna/delete/<%= question.question_id %>" method="POST" onsubmit="return confirm('정말 삭제하시겠습니까?');">
        <button id="delBtn" type="submit">질문 삭제</button>
      </form>
    </div>

  </div>
</body>

</html>