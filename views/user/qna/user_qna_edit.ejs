<!DOCTYPE html>
<html lang="ko">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>
    <%= locals.title %>
  </title>
  <link rel="stylesheet" href="/css/communityFunction_style.css">
</head>

<body>
  <div id="add">
    <h1>
      <%= locals.title %>
    </h1>
    <div id="cover">
      <form action="/qna/edit/<%= question.question_id %>" method="POST">
        <div>
          <input type="text" id="title" name="title" value="<%= question.title %>" required>
        </div>
        <div>
          <textarea id="question" name="question" required><%= question.question %></textarea>
        </div>
        <div id="btnBox">
          <button id="editBtn" type="submit">수정</button>
          <button id="backBtn" type="button" onclick="location.href='/qna/detail/<%= question.question_id %>'">뒤로가기</button>
        </div>
      </form>
    </div>
  </div>
</body>

</html>