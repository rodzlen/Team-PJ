<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= locals.title %></title>
  <link rel="stylesheet" href="/css/freeboardDetail_style.css"> <!-- CSS 파일 임포트 -->
</head>
<div id="detail">
    <h3><%= data.title %></h3>
    <div id="cover">
    <textarea><%= data.content %></textarea>
    <img><%= data.image %></img>
    <p>작성일: <%= data.post_date %></p>
    <div id="btnBox">
    <button id="backBtn" onclick="location.href='/freeboard'">목록으로</button>
    <button id="editBtn" onclick="location.href='/freeboard/edit/<%= data.id %>'">수정하기</button><!--로그인 시 세션에 저장된 고유 식별에 따라 버튼 보이기-->
    <form action="/freeboard/delete/<%= data.id %>" method="POST" onsubmit="return confirm('정말 삭제하시겠습니까?');">
        <button id="delBtn" type="submit">삭제</button>
    </form>
  </div>
    </div>
</div>