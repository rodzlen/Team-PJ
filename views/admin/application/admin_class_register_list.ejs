<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= locals.title %></title>
    <link rel="stylesheet" href="/css/application_style.css"> <!-- CSS 파일 임포트 -->
</head>
<body>
    <div class="FreeBoard-box">
        <h3><%= locals.title %></h3>
        <form action="/admin/classRegList" method="GET">
            <input type="text" name="search" placeholder="검색">
            <select name="type">
                <option value="no">no</option>
                <option value="createBy">신청자</option>
                <option value="no||createBy">no+신청자</option>
            </select>
            <button type="submit">검색</button>
        </form>
        <table>
            <thead>
                <tr>
                    <th>no</th>
                    <th>신청자</th>
                    <th>작성일</th>
                </tr>
            </thead>
            <tbody>
                <% locals.classReg.forEach((reg) => { %>
                <tr>
                    <td><%= reg.id %></td>
                    <td><a href="/admin/classRegList/detail/<%= reg.id %>"><%= reg.owner_name %></a></td>
                    <td><%= reg.created_at %></td>
                </tr>
                <% }) %>
            </tbody>
        </table>
    </div>
</body>
</html>
