<!DOCTYPE html>
<html lang="ko">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>관리자 강아지 정보 수정 페이지</title>
  <link rel="stylesheet" href="/css/dashboard_style.css" />
</head>

<body>
  <div id="dashboard">
    <form action="/admin/dashboard/admin_edit/<%= post.dog_id %>" method="POST" enctype="multipart/form-data">
      <div class="admin-title">
        <h2>
          <%= title %>
        </h2>
      </div>
      <div id="cover">
        <div class="flexBox">
          <div class="label">강아지 사진 &nbsp;&nbsp; </div>
          <input type="file" name="photo" id="photo" />
          <img src="/img/<%= post.dog_photo %>" alt="강아지 사진" style="max-width: 200px" />
        </div>
        <div class="flexBox">
          <div class="label">강아지 이름 &nbsp;&nbsp; </div>
          <input type="text" name="pet_name" id="pet_name" value="<%= post.pet_name %>" readonly />
        </div>
        <div class="flexBox">
          <div class="label">주인 이름 &nbsp;&nbsp; </div>
          <input type="text" name="owner_name" id="owner_name" value="<%= post.owner_name %>" readonly />
        </div>
        <div class="flexBox">
          <div class="label">날짜 &nbsp;&nbsp; </div>
          <input type="date" name="walk_date" id="walk_date" value="<%= post.walk_date %>" />
        </div>
        <div class="flexBox">
          <div class="label">산책 시작 시간 &nbsp;&nbsp; </div>
          <input type="time" name="walk_starttime" id="walk_starttime" value="<%= post.walk_starttime %>" />
          <div class="label">산책 끝 시간 &nbsp;&nbsp; </div>
          <input type="time" name="walk_endtime" id="walk_endtime" value="<%= post.walk_endtime %>" />
        </div>
        <div class="flexBox">
          <div class="label">오늘의 산책 사진 &nbsp;&nbsp; </div>
          <input type="file" name="walk_photo" id="walk_photo" />
          <img src="/img/<%= post.walk_photo %>" alt="오늘의 산책 사진" style="max-width: 200px" />
        </div>
        <div class="flexBox">
          <div class="label">담당자 이름 &nbsp;&nbsp; </div>
          <input type="text" name="teacher" id="teacher" value="<%= post.teacher %>" />
        </div>
        <div class="flexBox">
          <div class="label">특이 사항 &nbsp;&nbsp; </div>
          <textarea name="note_info" id="note_info" cols="50" rows="10">
<%= post.note_info %></textarea>
        </div>
        <div class="flexBox">
          <div class="label">수업 정보 &nbsp;&nbsp; </div>
          <textarea name="class_info" id="class_info" cols="50" rows="10">
<%= post.class_info %></textarea>
        </div>
        <div class="flexBox">
          <div class="label">급식 신청 &nbsp;&nbsp; </div>
          <label>
            <input type="radio" id="feedYes" name="feed" value="1" <%=post.feed ? 'checked' : '' %>> 예
          </label>
          <label>
            <input type="radio" id="feedNo" name="feed" value="0" <%=!post.feed ? 'checked' : '' %>> 아니오
          </label>
        </div>
      </div>

      <div id="btnBox">
        <div>
          <button id="editBtn" type="submit" onclick="return confirm('정말 수정하시겠습니까?')">수정</button>
          <!-- <input type="submit" value="수정" class="btn" onclick="return confirm('정말 수정하시겠습니까?')" /> -->
        </div>
    </form>
    <!--delete 폼-->
    <form action="/admin/dashboard/delete/<%= post.dog_id %>" method="POST">
      <button id="delBtn" type="submit" onclick="return confirm('정말 삭제하시겠습니까?')">삭제</button>
    </form>
    <!-- <input type="submit" value="삭제" class="btn-delete btn" onclick="return confirm('정말 삭제하시겠습니까?')" /> -->
    <button id="backBtn" type="submit" onclick="window.location.href='/admin/class/admin_postlist'">뒤로</button>
  </div>
  <!-- <input type="button" value="뒤로" class="btn" onclick="window.location.href='/admin/class/admin_postlist'" />
    </form> -->
  </div>
</body>

</html>

<!-- views/admin/dashboard/admin_edit -->